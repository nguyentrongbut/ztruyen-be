export enum ProviderType {
  LOCAL = 'local',
  GOOGLE = 'google',
  FACEBOOK = 'facebook',
}

export enum RoleType {
  ADMIN = 'admin',
  AUTHOR = 'author',
  USER = 'user',
}

export enum GenderType {
  MALE = 'male',
  FEMALE = 'female',
  LGBT = 'lgbt',
}
